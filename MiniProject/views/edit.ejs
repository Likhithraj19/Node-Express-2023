<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="px-3 py-2 bg-red-500 rounded-md text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>

        <h5 class="mb-5">Edit Your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on Your mind?" class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block" name="content"><%= post.content %></textarea>
            <input type="submit" class="text-sm text-black px-3 w-40 py-2 bg-yellow-500 block rounded-md mt-2" value="Update Post">
        </form>
      
    </div>
</body>
</html>